export enum FormFieldType {
  INPUT = 'input',
}

export enum Role {
  ADMIN = 'admin',
  CUSTOMER = 'customer',
  MANAGER = 'manager',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}
